GOOGLE CLOUD:
 - To use Cloud SQL Proxy, copy cloud_sql_proxy_x64.exe to gcloud root dir

TRAVIS CI:
 - travis encrypt-file FILE -add #needs to be on linux, use a Mac instead
 - need to give Travis CI google IAM the App Engine Admin and Cloud Build Service Account roles
 - make sure skip_cleanup = True

INCOMPLETE
 - environment variable  must be set in local, travis, gae (trouble with gae, not using env vars for now)
